<Page x:Class="DnDTool2.View.SpellTome"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DnDTool2.View"
      mc:Ignorable="d" 
      d:DesignHeight="690" d:DesignWidth="1280"
      Title="SpellTome">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Button Content="Add Spell" HorizontalAlignment="Center" VerticalAlignment="Top" Command="{Binding OpenAddSpellCommand}"/>

        <DataGrid Grid.Column="1" ItemsSource="{Binding Spells}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Spell Name" Binding="{Binding Name}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
